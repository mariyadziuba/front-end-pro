//Дано ціле число (ввести через 'prompt'). 
//З'ясувати, чи просто воно (простим називається число, більше 1, що не має інших дільників, крім 1 і себе).

let wholeNumb = prompt ( `Введіть просте число `);
let n = Number(wholeNumb);

if(isNaN(n)){
	console.log(`Ви ввели не просте число`);
}
else (n<=1 && n/n == 1){
    console.log(`Ви ввели просте число `);
}